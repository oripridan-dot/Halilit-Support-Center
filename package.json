{
  "name": "halilit-support-center-root",
  "version": "9.5.0",
  "scripts": {
    "install:all": "pip install -r backend/requirements.txt && cd frontend && pnpm install",
    "dev": "PYTHONPATH=. python -m uvicorn backend.server:app --reload --host 0.0.0.0 --port 8000 & cd frontend && pnpm dev",
    "dev:backend": "PYTHONPATH=. python -m uvicorn backend.server:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && pnpm dev",
    "prod": "PYTHONPATH=. python -m uvicorn backend.server:app --host 0.0.0.0 --port 8000",
    "skeleton-sync": "PYTHONPATH=. python3 backend/skeleton_sync.py"
  }
}
