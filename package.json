{
  "name": "halilit-support-center-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "pip install -r backend/requirements.txt && cd frontend && pnpm install",
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "python backend/conductor_main.py server",
    "dev:frontend": "cd frontend && pnpm dev",
    "codegen": "cd backend && python scripts/generate_ts_types.py"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
